name: Molecule Test

on:
  push:
    branches:
      - master
      - test
      - create-workflow/*
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os:
          - ubuntu-18.04
        python-version:
          - 3.6
        # test-version:
        #   - 4.2
        #   - 4.0
        #   - 3.6
        #   - 3.4
        #   - 3.2
    steps:
      - uses: actions/checkout@v2.1.0
        # with:
        #   path: molecule_demo
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: python3 -m pip install 'molecule[docker]'
      - name: Run mongodb 4.2 test
        run: molecule test --scenario-name 4.2
      # - name: Run mongodb ${{test-version}} test
      #   run: molecule test --scenario-name ${{matrix.test-version}}
      # - name: Run mongodb 3.0 test
      #   run: molecule test --scenario-name 3.0